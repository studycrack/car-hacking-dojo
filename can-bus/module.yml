name: The CAN Bus

resources:
  - type: markdown
    name: Controller Area Network
    expandable: false
    content: |
      **CAN** is a two-wire broadcast bus. A frame carries an 11-bit
      *identifier* and up to 8 bytes of data, and that is very nearly the whole
      protocol. The identifier does not say who sent the frame or who should
      receive it. It says what the frame *means*, and every controller on the
      bus decides for itself whether it cares.

      There is no source address to check, so there is nothing to authenticate
      against. Every attack in this module follows from that one fact.

      Your workspace has a CAN interface named `vcan0` attached to a running
      vehicle, and the standard Linux CAN tooling to work with it: `candump` to
      listen, `cansend` to transmit.

  - type: header
    content: Reading the bus

  - id: sniffing
    type: challenge
    name: level1

  - id: filtering
    type: challenge
    name: level2

  - id: fob-capture
    type: challenge
    name: level3

  - type: header
    content: Replaying and forging frames

  - id: injection
    type: challenge
    name: level4

  - id: contention
    type: challenge
    name: level5

  - id: rolling-code
    type: challenge
    name: level6

  - id: resync
    type: challenge
    name: level7

  - type: header
    content: Working out what the bytes mean

  - id: spoofing
    type: challenge
    name: level8

  - id: dbc
    type: challenge
    name: level9

  - type: header
    content: Defeating a checksum and a MAC

  - id: integrity
    type: challenge
    name: level10

  - id: secoc
    type: challenge
    name: level11
